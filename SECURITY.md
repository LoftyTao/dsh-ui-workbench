# Security policy

Please do not report vulnerabilities in public issues. Instead, contact the repository maintainers privately once the GitHub repository contact is configured, and include reproduction steps plus the affected version.

This plugin serves session-scoped filesystem and Git data to the local DSH web UI. Reports involving path traversal, cross-session data access, command execution, or secrets exposure are especially welcome.
